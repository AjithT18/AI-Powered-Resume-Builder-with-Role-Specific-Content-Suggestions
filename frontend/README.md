# EliteResume Frontend

React + Vite + Tailwind CSS frontend for EliteResume.

## Setup

```bash
cd frontend
npm install
npm run dev
```

Create `.env` from `.env.example` and set:

- `VITE_API_BASE_URL`
- `VITE_GOOGLE_CLIENT_ID`
